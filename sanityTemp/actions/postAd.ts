// Temporary postAd action for build compatibility
export const postAd = async (adData: any) => {
  // Placeholder implementation
  return { success: true, id: 'temp-id' };
};

export default postAd;
