import PostSection from "@/app/(articles)/components/PostSection";

interface PageProps {
  
}

const Page = ({}: PageProps) => {
  return (
    <div>
        <PostSection />
    </div>
  );
};

export default Page;