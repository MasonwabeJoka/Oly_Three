import styles from "./Finish.module.scss"
import { FormWrapper } from "./FormWrapper";

const Finish = () => {
  return (
    <FormWrapper title="Finish">Finish</FormWrapper>
  );
};

export default Finish;